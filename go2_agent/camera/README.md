# Camera

This directory is reserved for future GO2 camera integration.

Phase4-A only provides read-only Intel RealSense D435i environment and ROS2
topic probe scripts under `go2_agent/tools/`. No camera code is connected to
the GO2 agent runtime, WebSocket, WebRTC, SLAM, or robot control.
